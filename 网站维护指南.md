# 课题组网站维护指南

**线上地址**：https://zbzhang85.github.io/zhang-research-group/
**代码仓库**：https://github.com/zbzhang85/zhang-research-group

---

## 一、网站内容都在哪里改？

几乎所有文字内容都集中在一个文件：

```
src/data/content.ts
```

用记事本 / VS Code 打开即可编辑。常见修改对照：

| 要改什么 | 在 content.ts 里找 |
|---|---|
| 首页 About Us 两段介绍 | `profileParagraphs` |
| 四个研究方向 + 代表论文 | `directions` |
| Group News 新闻 | `news` |
| 发表文章（三组） | `independentPubs` / `submittedPubs` / `priorPubs` |
| 会议报告 | `internationalTalks` / `domesticTalks` |
| 团队成员（PI/员工/博士/硕士） | `pi` / `staff` / `phdStudents` / `masterStudents` |
| 校友及去向 | `alumni` |
| 课程、荣誉 | `courses` / `honors` |
| 首页顶部统计数字 | `stats` |

**格式注意事项：**
- 每条内容用英文单引号 `'...'` 包起来，条目之间用逗号分隔
- 文章条目中期刊名会自动变斜体、`Z.B. Zhang` 会自动加粗下划线，无需手动设置
- 如果文字里本身有单引号（如 `Xi'an`），写成 `Xi\'an`

## 二、怎么换照片？

照片都放在 `public/images/` 下：

- 成员照片：`public/images/team/姓名拼音.jpg`（如 `xu-hansen.jpg`）
- 合影：`public/images/group-1.jpg`、`group-2.jpg`
- Logo：`public/images/logo.png`

**换照片 = 用同名文件覆盖即可**（建议 JPG、小于 500KB）。新增成员时，把照片放进 `team/` 文件夹，并在 content.ts 的成员条目里写 `photo: '姓名拼音'`（不带 .jpg）。

## 三、本地预览（改完先看效果）

打开 **Git Bash**，进入项目目录：

```bash
cd "/c/Users/zbzha/Dropbox/My PC (ZhenboZhang-ShanghaiTech)/Documents/ShanghaiTech/Publications/3D print NT Ti3662/zhang-research-group"
export PATH="/c/Program Files/nodejs:$PATH"
npm run dev
```

然后用浏览器打开终端里显示的地址（一般是 http://localhost:3000）。改内容保存后页面会自动刷新。按 `Ctrl + C` 停止预览。

## 四、发布上线（一条命令）

确认预览没问题后：

```bash
# 1. 提交源码备份到 main 分支
git add -A && git commit -m "更新说明" && git push origin main

# 2. 一键发布
npm run deploy
```

1–2 分钟后线上自动更新。

## 五、常见问题

**1. 推送失败 / 网络错误**
国内访问 GitHub 偶尔不稳定，重试一次一般就好（deploy 脚本已内置自动重试 3 次）。

**2. 发布成功但网页没变化**
- 等 1–2 分钟（GitHub 构建需要时间）
- 强制刷新浏览器：`Ctrl + Shift + R`

**3. 提示 npm 不是命令**
先执行 `export PATH="/c/Program Files/nodejs:$PATH"` 再运行 npm 命令。

**4. 改错了想恢复**
GitHub 仓库里有全部历史版本，随时可以找回；也可以直接找我帮忙。

**5. 不想自己操作**
随时把要改的内容发给 Kimi，改完会自动帮你部署上线。
